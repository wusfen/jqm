# $.js
jquery 的超轻量实现

```javascript
$('selector')
    .each()
    .closest()
    .delay()
    .addClass()
    .removeClass()
    .show(animateClass) // 可使用css3动画，比如使用 animate.css: .show('animated fadeIn')
    .hide(animateClass)
    .css()
    .on()
```
